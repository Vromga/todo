import { SearchTodoPipe } from './search-todo.pipe';

describe('SearcTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
